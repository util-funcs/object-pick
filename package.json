{
  "name": "@util-funcs/pick",
  "version": "0.0.1-beta.1",
  "description": "Pick properties from an object and take new one.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "dist",
    "lib/**/*"
  ],
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "prepare": "npm run build",
    "prepublishOnly": "npm test && npm run lint",
    "preversion": "npm run lint",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/atayahmet/pick.git"
  },
  "keywords": [
    "object",
    "slider"
  ],
  "author": "Ahmet ATAY",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/atayahmet/pick/issues"
  },
  "homepage": "https://github.com/atayahmet/pick#readme",
  "devDependencies": {
    "prettier": "^1.18.2",
    "tslint": "^5.19.0",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.6.2"
  },
  "dependencies": {}
}
